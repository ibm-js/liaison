// To run the test cases: http://yourserver/path/to/liaison/tests/sandbox/client.html?config=/path/to/liaison/tests/sandbox/intern
define({
	loader: {
		baseUrl: "../../..", // intern directory should be a sibling of liaison directory
		packages: ["liaison"]
	}
});
